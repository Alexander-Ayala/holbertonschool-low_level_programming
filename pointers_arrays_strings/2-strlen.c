#include "main.h"
#include <string.h>
/**
* _strlen - change values integedr
* @*s: pointer string
* @s: string
* Return: length
**/
int _strlen(char *s)
{
int leng;
leng = strlen(s);
return (leng);
}
