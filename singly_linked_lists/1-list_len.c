#include <stdio.h>
#include "lists.h"
/**
 * list_len - count elements of list_t
 * @h: pointer node
 * Return: number
 */
size_t list_len(const list_t *h)
{
unsigned int i;

for (i = 0; h; i++)
{

s = h->next;
}
return (i);
}
